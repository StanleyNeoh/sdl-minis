# Stamper practice with SDL2

## Setup
1. Rune `make run` to build and run the stamper application.

## Features
- Click anywhere in the window to place a stamp (an image of a stamp).
- Hold the 'z' to toggle delete mode and click on a stamp to remove it.
